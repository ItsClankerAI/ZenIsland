# Personal Hub

Personal Hub is the custom extension for this local ZenBar build. It shows time, date, a now-playing glance, and two configurable quick links from the notch.

Default quick links:

- Primary: Google Calendar
- Secondary: Apple Music

Change the URLs in ZenBar Settings -> Extensions -> Personal Hub.
